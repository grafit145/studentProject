package javacourse.studentorder.validator;

import javacourse.studentorder.domain.AnswerStudent;
import javacourse.studentorder.domain.StudentOrder;

public class StudentValidator {
    public static AnswerStudent checkStudent(StudentOrder so) {
        System.out.println("StudentCheck is running");
        AnswerStudent student = new AnswerStudent();
        return student;

    }
}
